# 11. Write a simple Python function that takes two numbers as arguments and returns their
# product (multiplication). Call the function with specific values and print the returned result.
def mul (a,b):
    return a * b
x = 10
y = 13
print(mul(x,y))